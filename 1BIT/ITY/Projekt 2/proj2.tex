%Druhy projekt do predmetu ITY 2013/2014
%Autor: Lukas Pelanek
%email: xpelan03@stud.fit.vutbr.cz


\documentclass[11pt, twocolumn, a4paper]{article}

\usepackage[cp1250]{inputenc}
\usepackage[IL2]{fontenc}
\usepackage{times}
\usepackage[left=1.5cm, text={18cm, 25cm}, top=2.5cm]{geometry}
\usepackage[czech]{babel}
\usepackage{alltt}
\usepackage{amsthm}
\usepackage{amssymb}
\usepackage{amsmath}

\theoremstyle{definition}
\newtheorem{definice}{Definice}[section]

\theoremstyle{definition}
\newtheorem{algoritmus}[definice]{Algoritmus}

\theoremstyle{definition}
\newtheorem{veta}{Vìta}

\begin{document}
\begin{titlepage}
\begin{center}
	\Huge
	\textsc{Fakulta informaèních technologií Vysoké uèení technické v~Brnì}\\
	\vspace{\stretch{0.382}}
	\LARGE
	 Typografie a publikování - 2. projekt \\ sazba dokumentù a matematickıch vırazù
	\vspace{\stretch{0.618}}
\end{center}
{\Large 2014 \hfill Lukáš Pelánek}
\end{titlepage}

\section*{Úvod}
V~této úloze si vyzkoušíme sazbu titulní strany, matematickıch vzorcù, prostøedí a dalších textovıch struktur obvyklıch pro technicky zamìøené texty (napøíklad rovnice \ref{rce} nebo definice \ref{def1} na stranì \pageref{def1}).

Na titulní stranì je vyuito sázení nadpisu podle optického støedu s~vyuitím zlatého øezu. Tento postup byl probírán na pøednášce.

\section{Matematickı text}
Nejprve se podíváme na sázení matematickıch symbolù a vırazù v~plynulém textu. Pro mnoinu $V$ oznaèuje $\mbox{card}(V)$ kardinalitu $V$.
Pro mnoinu $V$ reprezentuje $V^*$ volnı monoid generovanı mnoinou $V$ s~operací konkatenace.
Prvek identity ve volném monoidu $V^*$ znaèíme symbolem $\epsilon$
Nech $V^+ = V^* - \{\epsilon\}$. Algebraicky je tedy $V^+$ volná pologrupa generovaná mnoinou $V$ s~operací konkatenace.
Koneènou neprázdnou mnoinu $V$ nazvìme \emph{abeceda}.
Pro $w \in V^*$  oznaèuje $|w|$ délku øetìzce $w$. Pro $W \subseteq V$ oznaèuje $\mbox{occur}(w, W)$ poèet vıskytù symbolù z~$W$ v~øetìzci $w$ a $\mbox{sym}(w, i)$ urèuje $i$-tı symbol øetìzce $w$; napøíklad $\mbox{sym}(abcd,3)=c$.

Nyní zkusíme sazbu definic a vìt s~vyuitím balíku \texttt{amsthm}.

\begin{definice} \label{def1} \emph{Bezkontextová gramatika} je ètveøice $G=(V, T, P, S)$, kde $V$ je totální abeceda, $T \subseteq V$ je abeceda terminálù, $S \in (V - T)$ je startující symbol a $P$ je koneèná mnoina \emph{pravidel}
tvaru $q\colon A \longmapsto a$, kde $A \in (V - T)$, $a \in V^*$ a $q$ je návìští tohoto pravidla. Nech $N = V - T$ znaèí abecedu neterminálù.
Pokud $q\colon A \longmapsto a \in P$ , $\gamma, \delta \in V^*$, $G$ provádí derivaèní krok z~$\gamma A \delta$  do $\gamma a \delta$ podle pravidla $q\colon A \longmapsto a$, symbolicky píšeme $\gamma A \delta \Rightarrow \gamma a \delta\ [q\colon A \longmapsto a]$ nebo zjednodušenì $\gamma A \delta \Rightarrow \gamma a \delta$. Standardním zpùsobem definujeme $\Rightarrow^m$, kde $m \geq 0$ . Dále definujeme 
tranzitivní uzávìr $\Rightarrow^+$ a tranzitivnì-reflexivní uzávìr $\Rightarrow^*$.
\end{definice}

Algoritmus mùeme uvádìt podobnì jako definice textovì, nebo vyuít pseudokódu vysázeného ve vhodném prostøedí (napøíklad \texttt{algorithm2e}).
\begin{algoritmus}
Algoritmus pro ovìøení bezkontextovosti gramatiky. Mìjme gramatiku $G = (N, T, P, S)$.
\begin{enumerate} 
\item \label{alg1} \emph{Pro kadé pravidlo $p \in P$ proveï test, zda $p$ na levé stranì obsahuje právì jeden symbol z~$N$ .}
\item \emph{Pokud všechna pravidla splòují podmínku z~kroku \ref{alg1}, tak je gramatika $G$ bezkontextová.}
\end{enumerate}
\end{algoritmus}

\begin{definice} Jazyk definovanı gramatikou $G$ definujeme jako $\mbox{L}(G)=\{w \in T^*|S\Rightarrow^* w\}$. 
\end{definice}

\subsection{Podsekce obsahující vìtu} 

\begin{definice}Nech $L$ je libovolnı jazyk. $L$ je \emph{bezkontextovı jazyk}, kdy a jen kdy $L=\mbox{L}(G)$, kde $G$ je libovolná bezkontextová gramatika.
\end{definice}

\begin{definice}Mnoinu $\mathcal L_{CF}=\{L|L$ je bezkontextovı jazyk$\}$ nazıváme tøídou bezkontextovıch jazykù.
\end{definice}

\begin{veta}\label{veta}
\emph{Nech $L_{abc} = \{a^n b^n c^n|n\geq 0\}$ Platí, e $L_{abc} \not\in L_{CF}$.}
\end{veta}

\begin{proof}
Dùkaz se provede pomocí Pumping lemma pro bezkontextové jazyky, kdy ukáeme, e není moné, aby platilo, co bude implikovat pravdivost vìty \ref{veta}.
\end{proof}

\section{Rovnice a odkazy}

Sloitìjší matematické formulace sázíme mimo plynulı text. Lze umístit nìkolik vırazù na jeden øádek, ale pak je tøeba tyto vhodnì oddìlit, napøíklad pøíkazem \verb|\quad|. 

$$
\sqrt[x^2]{y_0^3}\quad\mathbb{N}=\{0,1,2,\ldots\}\quad x^{y^y}\neq x^{yy}\quad z_{i_j}\not\equiv z_{ij}
$$

V~rovnici (\ref{rce}) jsou vyuity tøi typy závorek s~rùznou explicitnì definovanou velikostí.

\begin{eqnarray}
\bigg\{\Big[(a+b) * c \Big]^d + 1 \bigg\}&=&x \label{rce} \\
\lim \limits_{ x \to \infty}\frac{\sin^2 x + \cos^2 x}{4}&=&y \nonumber
\end{eqnarray}

V~této vìtì vidíme, jak vypadá implicitní vysázení limity $\lim_{n \to \infty}f(n)$ v~normálním odstavci textu. Podobnì je to i s~dalšími symboly jako $\sum_1^n$ èi $\bigcup_{A\in\mathcal B}$. V~pøípadì vzorce $\lim \limits_{x \to 0} \frac{\sin x}{x}$ jsme si vynutili ménì úspornou sazbu pøíkazem \verb|\limits|.

\begin{eqnarray}
\int \limits_a^b f(x)\,\mathrm{d}x &=&- \int_b^a f(x)\,\mathrm{d}x\\
\left(\sqrt[5]{x^4}\right)' = \left(x^{\frac{4}{5}}\right)'&=&\frac{4}{5}x^{-\frac{1}{5}} = \frac{4}{5\sqrt[5]{x}}\\
\overline{\overline{A\vee B}}&=&\overline{\overline{A}\wedge \overline{B}}
\end{eqnarray}

\section{Matice}

Pro sázení matic se velmi èasto pouívá prostøedí \texttt{array} a závorky (\verb|\left|, \verb|\right|)

$$\left(\begin{array}{cc}
a+b&b-a\\
\widehat{\xi + \omega}&\hat{\pi}\\
\vec{a}&\overleftrightarrow{AC}\\
0&\beta
\end{array}\right)$$

$$\mathbf{A}= \left\|\begin{array}{cccc}
a_{11}&a_{12}&\ldots&a_{1n}\\
a_{21}&a_{22}&\ldots&a_{2n}\\
\vdots&\vdots&\ddots&\vdots\\
a_{m1}&a_{m2}&\ldots&a_{mn}
\end{array}\right\|$$

$$\left|\begin{array}{cc}
t&u\\
v&w
\end{array}\right|=tw - uv$$

Prostøedí \texttt{array} lze úspìšnì vyuít i jinde.

$$\binom{n}{k}=\left\{\begin{array}{l l}
\frac{n!}{k!(n-k)!}&\quad \mbox{pro $0 \leq k \leq n$}\\
0&\quad \mbox{pro $k < 0$ nebo $k > n$}
\end{array}\right. $$

\section{Závìrem}

V~pøípadì, e budete potøebovat vyjádøit matematickou konstrukci nebo symbol a nebude se Vám daøit jej nalézt v~samotném {\LaTeX}u, doporuèuji prostudovat monosti balíku maker {\AmS}-{\LaTeX}.
Analogická pouèka platí obecnì pro jakoukoli konstrukci v~{\TeX}u.

\end{document}
